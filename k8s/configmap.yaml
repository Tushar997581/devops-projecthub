apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudmart-config
  namespace: cloudmart

data:
  NODE_ENV: "production"

  PORT: "5000"

  DB_HOST: "cloudmart-db"

  DB_PORT: "3306"

  DB_NAME: "cloudmart"

  AWS_REGION: "ap-south-1"